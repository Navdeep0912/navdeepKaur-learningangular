import { WorldItemNamePipe } from './world-item-name.pipe';

describe('WorldItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WorldItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
